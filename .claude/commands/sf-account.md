---
description: Account view — details + contacts + engagement for an account
---

Show full account details including contacts, engagement, and top facilities.

Arguments: $ARGUMENTS

Provide an account/system name (partial match supported).

```bash
cd /Users/oliviagao/project/glytec/salesforge && source /Users/oliviagao/project/glytec/.venv/bin/activate && python ops/account_view.py $ARGUMENTS
```

After running, summarize the account's tier, key contacts, and engagement status.
